name: Babellint
on: [push]

jobs:
  babellint:
    name: Babellint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Babellint
        uses: heussd/babellint@main

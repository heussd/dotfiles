#!/bin/bash
set -o errexit
set -o pipefail
set -o nounset

mkdir ~/tempa
cd ~/tempa || exit 1

git clone ~/Documents/ .
cp /Volumes/Cl*/Do*/P*x .

git add -A -f
git commit -m "Added before bundle"

git bundle create ~/.minib --all

cd .. || exit 1
rm -R ~/tempa

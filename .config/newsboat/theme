# https://newsboat.org/releases/2.24/docs/newsboat.html#_configuring_colors
# color <element> <foreground color> <background color> [<attribute> ...]

# a normal list item
color listnormal            default default

# the currently selected list item
color listfocus             yellow default

# an unread list item
color listnormal_unread     default default

# the currently selected and unread list item
color listfocus_unread       yellow default

# the info bars on top and bottom
color info                  default default

# the application background
color background            default default

# the article text
color article               default default

# filler lines (~) below blocks of text
color end-of-text-marker    default default


highlight article "^(Title):.*$"	blue default
highlight article "(matches .*)$" 	red  default

#!/bin/bash
set -o errexit
set -o pipefail
set -o nounset


image=$(grep -Eo "https?://[a-zA-Z0-9./?=_%:-]*png" < /dev/stdin)

clear
kitty +kitten icat --hold --scale-up --place "50x50@0x0" "$image"
clear

git-stat-folder() { [ -d "$1" ] && printf " %-9s @ %s\n" "$1" "$(git-head-stat $1)"; }
function _hasCmd() { type "$1" &>/dev/null; }

# Console prints may be skipped
if [ "$1" != "silent" ]; then

	make

	_hasCmd gita && (
		gita ll
		echo
	)

	_hasCmd docker && (
		docker ps --format "table {{.Image}}\t{{.Status}}\t{{.Ports}}"
		echo
	)

fi

# Console prints may be skipped
if [ "$1" != "silent" ]; then

	make

	_hasCmd gita && (
		gita ll
		echo
	)

	make auto-install
fi

# Try to resume screen session
_hasCmd screen && (
	if [ $TERM != 'screen' ]; then
	  screen -r > /dev/null
	fi
)
#!/bin/bash
set -o errexit
set -o pipefail
set -o nounset

open -a "Dozer.app"
open -a "Tiles.app"
open -a "Maccy.app"
open -a "Hammerspoon.app"
open -a "Easy Move+Resize.app"
open -a "Docker.app"
open -a "Next Meeting.app"
